{
  "name": "d2l-button-group",
  "description": "Polymer-based web component to responsively encapsulate a group of buttons and drowdowns",
  "private": true,
  "scripts": {
    "postinstall": "polymer install --variants",
    "lint": "npm run lint:html && npm run lint:wc",
    "lint:html": "eslint *.html demo/*.html test/*.html",
    "lint:wc": "polymer lint",
    "test": "npm run lint && polymer test --skip-plugin sauce"
  },
  "author": "D2L Corporation",
  "license": "Apache-2.0",
  "devDependencies": {
    "eslint": "^4.5.0",
    "eslint-config-brightspace": "^0.3.1",
    "eslint-plugin-html": "^3.1.0",
    "polymer-cli": "^1.5.4"
  }
}
